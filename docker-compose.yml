nginx:
    image: nginx:alpine
    links:
        - fpm
    ports:
        - "8888:80"
    volumes:
        - ./nginx.conf:/etc/nginx/conf.d/default.conf
        - ./:/var/www/html

fpm:
    image: yannoff/php-fpm:7.1-fpm-alpine
    volumes:
        - ./:/var/www/html

    ports:
        - 9000
